# Colab requirements — torch/torchvision/torchaudio are pre-installed with CUDA support,
# so we skip them here to avoid overwriting the CUDA build with a CPU build.

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0
audioread>=3.0.0
resampy>=0.4.2          # required by torch.hub VGGish (FAD); without it FAD falls back to random-init embeddings

# MIDI Processing
pretty_midi>=0.2.10
mido>=1.3.0

# Source Separation
demucs>=4.0.0

# Vocoder
bigvgan>=2.4.0

# Visualization
matplotlib>=3.7.0

# Google Drive API
google-api-python-client>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=0.8.0

# YouTube Download
yt-dlp>=2024.1.0

# Scientific Computing
numpy>=1.24.0
scikit-learn>=1.2.0
numba>=0.57.0
